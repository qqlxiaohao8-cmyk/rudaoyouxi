{
  "name": "rudao-cultivation-sim",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && node scripts/ensure-dist.mjs && wrangler pages deploy dist --project-name=rudao-cultivation-sim",
    "deploy:worker": "npm run build && node scripts/ensure-dist.mjs && wrangler deploy --config wrangler.worker.toml"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "wrangler": "^4.90.0"
  }
}
